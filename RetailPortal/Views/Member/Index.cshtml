﻿@{
    ViewData["Title"] = "Member Details";
    var membersModel = (Members)Model;
}

<div class="card mt-5">
    <!-- Welcome Message -->
    @* <div class="row">
        <div class="col-md-12">
            <p class="welcome-message">Welcome, @ViewBag.SponsorName</p>
        </div>
    </div>

    <!-- Navigation Tabs -->
    <ul class="nav nav-tabs">
        <li class="nav-item">
            <a class="nav-link" href="/SponsorDetails">Sponsor & Policy Details</a>
        </li>
        <li class="nav-item">
            <a class="nav-link active" href="/MemberDetails">Member Details</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="/ProductDetails">Product Details</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="/Payment">Payment</a>
        </li>
    </ul> *@
    <div class="progress " role="progressbar" aria-label="Animated striped example" aria-valuenow="50" aria-valuemin="0" aria-valuemax="100" style="height: 30px">
        <div class="progress-bar progress-bar-striped progress-bar-animated" style="width: 50%"></div>
    </div>
    <ul class="nav nav-tabs nav-justified">
        <li class="nav-item">
            <a class="nav-link " aria-current="page" href="#"><i class="bi bi-1-square"></i><span class="ms-1 text-primary">Add Sponsor</span></a>
        </li>
        <li class="nav-item">
            <a class="nav-link active" aria-disabled="true">Add Members</a>
        </li>
        <li class="nav-item">
            <a class="nav-link disabled" aria-disabled="true">Choose Benefit</a>
        </li>
        <li class="nav-item">
            <a class="nav-link disabled" aria-disabled="true">Payment</a>
        </li>
    </ul>
    <form id="memberForm" method="post" action="@Url.Action("InsertMembers", "Member")">
        <input type="hidden" name="BrokerId" value="@Model.BrokerId" />

        <!-- Member Details Section -->
        <div class="row justify-content-center mt-4">
            @* <div class="col-md-12">
                @(Html.Partial("_MemberDetails", membersModel))
            </div> *@
            <div class="col-md-8">
                @(await Html.PartialAsync("_MemberAdd", membersModel))
            </div>
        </div>

        <!-- Agent Details Section - Below Member Details -->
        @* <div class="row mt-4">
            <div class="col-md-12">
                @(Html.Partial("_AgentDetails", membersModel))
            </div>
        </div> *@

        <!-- Submit and Next Buttons -->
        <div class="container text-end">
            <div class="row m-sm-3">
                <div class="col-sm-3 col-xl-6">&nbsp</div>
                <button type="submit" class="col-sm-3 col-xl-2 btn btn-lg btn-outline-danger">Save</button>
                <div class="col-sm-6 col-xl-4 d-grid">
                    <a href="@Url.Action("_ProductDetails", "Product")" class="btn btn-lg btn-outline-primary">
                        Next
                    </a>
                </div>
            </div>
        </div> 
    </form>
</div>

<script>
    function clearForm() {
        document.getElementById('memberForm').reset();
        document.getElementById('memberchoose').value = 'select';
        document.getElementById('memberrelation').value = 'none';
    }
</script>